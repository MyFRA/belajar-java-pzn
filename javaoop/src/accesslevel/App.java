package accesslevel;

public class App {
    //  ada public, protected, no modifier dan private
    //  public → bebas diakses dari mana saja
    //  protected → satu package + subclass
    //  default → hanya satu package
    //  private → hanya dalam satu class
}
